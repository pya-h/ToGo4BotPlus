## Prior Features
* Rapid Input; A way to input togos quickly in order, user specifies the Starting time and date, and Then adds tasks without needing to specify time, date;
    It just requires short commands with duration in between. This mode continues until user triggers Stop command.
* Add periodical Togos; Items that should be listed everyday, or on a specific interval.

* Add pagination to inline menues too; Telegram doesn't allow more than 100 buttons inside a ReplyMarkeup

* Add Port To Tommorrow section: Lists yesterday's remaining tasks and moves them to tomorrow on selection.

## Important Todos:
* Extra notification for Long tasks; e.g. Resend notification after 1 hours on a 2 Hours task.

* Separate normal bot input mode, alongside command mode; User can create their Togosstep by step by normal Bot Inputs, buttons and etc.

* Extra Statistical & Progress Parameters

* Handle pagination and filtering logics all with queries;
    Otherwise it will cause on high loads; Although this is a small system but whatever.

* Write a tick command like 'tk  id', make ticking easier.

* Implement Multiple Selection like: $  id1  id2  ...

* Checkout Togo package and Revise all its unclean or bad parts.